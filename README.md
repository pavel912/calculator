# AST Calculator
Calculator that uses AST (Abstract Syntax Tree) to parse, evaluate and analyze simple mathematical expressions:

Example:

```
Input: (((1 +2) * (3/ 4 ))^(1/2) - (10 + 1 - 5 *(-7)))
Output: -44.5
```
